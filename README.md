# AllegroLikeServer

## Setting up
1. Create `db.env` file in root and fill it accordingly:
```env
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=AllegroLikeDb
```
2. Run docker compose with the db -> `docker compose up -d`